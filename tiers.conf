# Cumulative tier hierarchy — lightest first, heaviest last.
# A source tagged at tier T is included in outputs for T and all heavier tiers.
# The tier marked * is the default — its outputs land at processed/ root.
# Sources without a tier tag default to the * tier.
light
good *
aggressive
